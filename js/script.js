$(document).ready(function(){
	$('.about__slider').slick({
		dots:true,
	});

	$('.esports__games-slider').slick({
		dots:true,
  		slidesToShow: 2,
  		slidesToScroll: 2
	});

	$('.teams__slider').slick({
		dots:true,
	});
});
